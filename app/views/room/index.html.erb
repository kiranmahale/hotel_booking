<p id="notice"></p>

<h1>My Rooms</h1>

<table>
  <thead>
    <tr>
      <th>Room No.</th>
      <th>Room Category</th>
      <th>Date In</th>
      <th>Date Out</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rooms.each_with_index do |room, index| %>
      <tr>
        <td><%= index + 1 %></td>
        <td><%= room.number %></td>
        <td><%= room.roomcategory.name %></td>
        <td><%= room.reservations.first.date_in %></td>
        <td><%= room.reservations.first.date_out %></td>
        <!-- <td><%#= link_to 'Show', u %></td>
        <td><%#= link_to 'Edit', edit_user_path(user) %></td>
        <td><%#= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to "Book A Room", new_reservation_path %>
